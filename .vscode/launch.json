{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Avalonia: ApoFisher",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/ApoFisher.csproj"
        }
    ]
}